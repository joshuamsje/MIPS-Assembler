// OpBits.h
#ifndef OPBITS_H
#define OPBITS_H
#include <stdio.h>
#include <inttypes.h>
#include <stdbool.h>

struct _OpBits {
	char* operation;
	char* opCode;
	
};

typedef struct _OpBits OpBits;

#endif
